# Agent instructions

- When a task is complete and verified (tests/typecheck green where
  applicable), commit the changed files and push to the current branch
  without waiting for the user to ask. Never amend, rebase, or
  rewrite history; leave unrelated changes alone.
