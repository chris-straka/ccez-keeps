org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Room schema export stays off: the D1 Drizzle schema (db/schema.ts) is the
# source of truth; Room mirrors it and the LWW parity tests guard semantics.
room.incremental=true
